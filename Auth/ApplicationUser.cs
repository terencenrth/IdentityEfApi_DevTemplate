using Microsoft.AspNetCore.Identity;

namespace IdentityEfApi.Auth
{
    public class ApplicationUser : IdentityUser
    {
        // Add profile fields if you want (e.g., FirstName, LastName)
    }
}
